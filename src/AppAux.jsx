import React from "react";
import Header from "./components/Header/Header";

const AppAux = () => {
  return <div>AppAux</div>;
};

export default AppAux;

// rafce = react arrow function component export

//criando um array usando uma variavel int
// const items = 5;
// {
//   items
//     ? Array(items)
//         .fill()
//         .map((currentValue, index) => <Component key={`${title}-${index}`} />)
//     : "b";
// }
